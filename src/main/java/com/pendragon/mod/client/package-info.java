/**
 * Client-only code: screens, renderers, key bindings, sound playback hooks.
 * Everything here must be safe to load only when {@code Dist.CLIENT}.
 * Populated from Fase 1 (character creation screen) onward.
 * Intentionally empty in Fase 0.
 */
package com.pendragon.mod.client;
